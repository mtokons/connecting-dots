import React from 'react';

const HostView: React.FC = () => {
  return (
    <div style={{ width: '100%', height: '100%', background: '#111', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <h2 style={{ color: '#fff' }}>Host View</h2>
    </div>
  );
};

export default HostView;
